{
  "name": "ejercicionode-1",
  "version": "1.0.0",
  "description": "eee?",
  "main": "index.js",
  "scripts": {
    "start": "npm start",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../ejercicio5 && npm run build && cp -r dist ../ejercicioNode-1",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "engines": {
    "node": "21.7.1"
  },
  "author": "yo",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
